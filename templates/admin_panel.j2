{% extends 'base.j2' %}

{% block head %}
{{ super() }}
<link rel="stylesheet" href="{{ url_for('static', filename='style/admin.css') }}" />
<title>{{ app_name }} Management Panel</title>
{% endblock %}

{% block body %}
<div class="admin-content">
    <div class="options-list">
        <div id="userDisplay">
            Logged in as <span class="username">{{ session["username"] }}</span>
        </div>
        <ul>
            {% set admin_sections = [("Site", "general"), ("Users", "users"), ("Plugins", "plugins"), ("Logs", "logs")]
            %}
            {% for section_name, fragment in admin_sections %}
            <a href="#{{fragment}}">
                <li
                    class="text-hoverable{% if loop.first %} options-list-top{% elif loop.last %} options-list-bottom{% endif %}">
                    {{section_name}}
                </li>
            </a>
            {% endfor %}
        </ul>
    </div>
    <div id="adminDisplay">
    </div>
</div>
{% endblock %}

{% block scripts %}
<script src="{{ url_for('static', filename='js/admin_panels.js') }}"></script>
{% endblock %}